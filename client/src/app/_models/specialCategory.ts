export interface SpecialCategory {
    autoId: number;
    code: string;
    description: string;
    createUserId?: number;
    createDateTime ? : string;
    updateUserUd ?: number;
    updateDateTime ?: string;
    //isActive: boolean;
}
