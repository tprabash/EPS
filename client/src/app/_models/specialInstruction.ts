export interface SpecialInstruction {
    autoId: number;
    description: string;
    createUserId?: number;
    createDateTime ? : string;
    updateUserUd ?: number;
    updateDateTime ?: string;
}

